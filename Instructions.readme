R e a d m e
-----------
基于AI块的主动弹制导脚本，ver - 20250904
需AI飞行，AI攻击，RC，陀螺仪和推进器即可工作，RC为导弹坐标系朝向
无需编组，只需要在导弹结构有变，保存蓝图之前运行save参数即可（第一次放置脚本时不需要）
当前支持无RC导弹，此种情况下，质量与货物重量估算可能不准确，同时无法获得重力读数，仅推荐小型导弹使用
默认情况下使用PB坐标系作为前向，
也可以将指定方块中的名称加入“代理”，将会使用该方块作为坐标系。请注意方块朝向。
自动配置弹头引爆序列-支持殉爆箱，
支持无传感器的近炸引信与碰炸引信，自动配置，默认值见自定义数据
支持传感器-但传感器不会自动配置探测距离与探测种类，逻辑为只要探测到就引爆弹头，弹头不需要手动配置
暂不支持摄像头
挂架发射：导弹上拥有连接器，合并方块，转子的任何一个。关闭（disable）任何一个/运行launch参数视为离架
分离推进器：名称中含有 分离 的推进器会作为分离推进器，在导弹离架时助推，默认为前推